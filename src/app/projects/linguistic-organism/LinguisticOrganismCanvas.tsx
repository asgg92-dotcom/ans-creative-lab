'use client';

// 빈 캔버스 컴포넌트: 새로운 아이디어를 위해 초기화됨
export default function LinguisticOrganismCanvas() {
  return (
    <div className="w-full h-full flex items-center justify-center bg-gray-100 text-gray-400">
      {/* 
        여기에 새로운 구현을 추가하세요.
        현재는 빈 상태입니다.
      */}
      <p>Ready for a new idea...</p>
    </div>
  );
}
